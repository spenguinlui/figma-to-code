# Visual Check Progress

格式：`[ ] <path> @ <breakpoint>` → 完成打勾 `[x]`，同檔三斷點都好就 commit。
斷點：D=1440px / T=768px / M=375px。`[N/A]` = 該斷點無對應 variant，跳過。
fileKey / canvas 從 `.env` 讀（`FIGMA_FILE_KEY` / `FIGMA_CANVAS_NODE_ID`）。

---

## Components

- [x] components/example/ExampleButton.tsx @ D

## Pages

- [x] pages/example/ExamplePage.tsx @ D
